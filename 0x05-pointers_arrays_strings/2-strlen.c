#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @s: string to evaluate
 * Return: the lenght of a string
*/

int _strlen(char *s)
{
	int x = 0;

	while (*s != '\0')
	{
		x++;
		s++;
	}
	return (X);
}
